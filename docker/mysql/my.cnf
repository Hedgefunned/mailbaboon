[mysqld]
# Allow LOAD DATA LOCAL INFILE for bulk contact imports
local_infile = ON

# Disable built-in stopwords so short but meaningful tokens (e.g. "com", "de")
# are indexed and searchable in FULLTEXT queries on the contacts table
innodb_ft_enable_stopword = OFF
